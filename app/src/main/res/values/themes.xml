<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Màu chữ cho tiêu đề và các mục menu -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="homeAsUpIndicator">@drawable/ic_back_arrow_custom</item>
        <!-- Màu nền của Toolbar -->
        <item name="android:background">#6E75FF</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:navigationBarColor">#6E75FF</item>
        <item name="homeAsUpIndicator">@drawable/ic_back_arrow_custom</item>
    </style>


</resources>